# DevTools Hub 技术架构设计

## 1. 项目定位

DevTools Hub 是一个面向开发者的跨平台原生效率工具，目标体验参考 uTools、Raycast、DevToys、Linear、PasteNow 和 VS Code。

核心目标：

- 不使用 Electron、WebView、Tauri
- 使用 Rust 构建跨平台原生应用
- 使用 Slint 构建低资源占用的现代化 GUI
- 支持 Windows、macOS、Linux
- 键盘优先，搜索即执行
- 内置常用开发工具
- 支持插件扩展
- 保持模块化、高内聚、低耦合

非目标：

- 不以内置浏览器能力作为插件 UI 基础
- 不在早期实现完整 IDE 级编辑器体验
- 不在早期开放不受限制的原生动态库插件生态
- 不为了插件生态引入大型运行时

## 2. 技术选型

### 2.1 核心语言

Rust。

选择原因：

- 内存安全
- 原生性能
- 跨平台能力强
- 适合系统服务、后台任务、插件宿主和本地工具开发

### 2.2 GUI 框架

Slint。

选择原因：

- 原生渲染
- 支持 Windows、macOS、Linux
- UI 描述和 Rust 业务逻辑分离
- 支持主题、动画、组件化
- 资源占用低于 Electron/WebView 类方案

需要重点验证：

- 快捷键唤起后的窗口显示速度
- 输入框焦点稳定性
- 大列表滚动性能
- 大文本展示和编辑能力
- 多窗口、托盘、置顶窗口的配合

### 2.3 数据存储

SQLite + rusqlite。

保存内容：

- 用户配置
- 剪贴板历史
- 插件信息
- 工具执行历史
- 搜索索引
- 收藏项

搜索建议优先使用 SQLite FTS5，避免早期自研搜索引擎。

### 2.4 异步运行时

Tokio。

使用场景：

- 剪贴板轮询或监听
- 后台任务调度
- 插件执行
- 索引更新
- 网络类内置工具
- 系统事件分发

Slint UI 线程和 Tokio 后台任务需要通过 channel 解耦。后台任务不能直接阻塞或修改 UI 状态，UI 更新应回到 Slint 事件循环中执行。

## 3. 总体架构

```text
DevTools Hub

├── UI Layer
│   └── Slint
│
├── Core Runtime
│   ├── Application Manager
│   ├── Command Router
│   ├── Search Engine
│   ├── Window Manager
│   ├── Tray Manager
│   ├── Event Bus
│   └── Permission Manager
│
├── System Services
│   ├── Clipboard Service
│   ├── Global Shortcut Service
│   ├── Notification Service
│   ├── File System Service
│   └── Shell Service
│
├── Storage Layer
│   ├── SQLite
│   ├── Repository
│   └── Migration
│
├── Plugin System
│   ├── Plugin Manifest
│   ├── Plugin Registry
│   ├── Plugin Host
│   ├── Plugin API
│   ├── WASM Plugin Runner
│   ├── Process Plugin Runner
│   └── Native Plugin Runner
│
└── Built-in Tools
    ├── JSON
    ├── JWT
    ├── Base64
    ├── URL Encode/Decode
    ├── Timestamp
    ├── UUID
    └── SQL Formatter
```

核心原则：

- UI 不直接访问数据库
- UI 不直接执行插件
- UI 不直接调用系统 API
- Core Runtime 负责命令分发和状态协调
- System Services 封装平台差异
- Storage Layer 封装持久化细节
- Built-in Tools 和 Plugins 通过统一 Command 接口接入搜索与执行

## 4. 推荐目录结构

采用 Cargo Workspace 拆分：

```text
devtools_hub/

├── Cargo.toml
├── crates/
│   ├── devtools-app/
│   │   └── 应用入口、依赖装配、启动流程
│   │
│   ├── devtools-ui/
│   │   └── Slint UI、UI Model、主题、组件
│   │
│   ├── devtools-core/
│   │   └── 应用状态、命令路由、搜索、事件总线
│   │
│   ├── devtools-storage/
│   │   └── SQLite、migration、repository
│   │
│   ├── devtools-services/
│   │   └── 剪贴板、快捷键、通知、托盘、系统能力
│   │
│   ├── devtools-plugin-api/
│   │   └── 插件协议、manifest、权限模型、数据结构
│   │
│   ├── devtools-plugin-host/
│   │   └── WASM/进程/原生插件宿主
│   │
│   └── devtools-tools/
│       └── JSON、JWT、Base64、时间、UUID 等内置工具
│
├── plugins/
│   └── 本地插件目录
│
├── migrations/
│   └── SQLite migration
│
├── assets/
│   └── 图标、主题资源
│
└── docs/
    └── 设计文档
```

## 5. 运行时模型

### 5.1 UI 主线程

Slint 运行在 UI 主线程，负责：

- 搜索窗口渲染
- 输入事件处理
- 列表展示
- 工具页面展示
- 设置页面展示

UI 线程只发送用户意图，不直接做耗时任务。

示例：

```text
用户输入
  ↓
UI 发送 SearchQuery
  ↓
Core Runtime 查询索引
  ↓
返回 SearchResult
  ↓
UI 更新列表
```

### 5.2 Tokio 后台运行时

Tokio Runtime 负责后台任务：

- 剪贴板采集
- 搜索索引更新
- 插件扫描
- 插件执行
- 文件 IO
- 网络请求
- 定时清理

后台任务通过 channel 向 Core Runtime 发送事件，再由 Core Runtime 统一更新应用状态。

### 5.3 事件总线

内部事件示例：

```rust
pub enum AppEvent {
    ShortcutPressed,
    ClipboardChanged(ClipboardItem),
    SearchRequested(SearchQuery),
    SearchCompleted(Vec<SearchResult>),
    CommandExecuted(CommandResult),
    PluginInstalled(PluginId),
    PluginFailed(PluginId, String),
    ThemeChanged(ThemeMode),
}
```

事件总线用于降低模块之间的直接依赖。

## 6. 核心流程

### 6.1 全局快捷启动

```text
全局快捷键
  ↓
Global Shortcut Service
  ↓
AppEvent::ShortcutPressed
  ↓
Window Manager
  ↓
显示 / 隐藏主搜索窗口
  ↓
输入关键词
  ↓
Search Engine
  ↓
匹配内置工具、插件、剪贴板、历史记录
  ↓
执行命令
```

默认快捷键建议：

- Windows/Linux: Alt + Space
- macOS: Option + Space 或 Command + Space 的替代组合

注意：Command + Space 在 macOS 通常被 Spotlight 占用，不建议默认抢占。

### 6.2 搜索即执行

搜索结果来源：

- 内置工具
- 插件命令
- 剪贴板历史
- 收藏项
- 最近执行
- 设置项

统一结果结构：

```rust
pub struct SearchResult {
    pub id: String,
    pub title: String,
    pub subtitle: Option<String>,
    pub icon: Option<String>,
    pub source: SearchSource,
    pub score: f32,
    pub action: CommandAction,
}
```

### 6.3 剪贴板管理

支持内容：

- 普通文本
- JSON
- SQL
- URL
- Token
- 日志
- 命令行片段

功能：

- 历史记录
- 搜索
- 收藏
- 删除
- 自动清理
- 敏感信息检测
- 去重

建议表结构：

```sql
CREATE TABLE clipboard_item (
    id INTEGER PRIMARY KEY,
    content TEXT NOT NULL,
    content_type TEXT NOT NULL,
    hash TEXT NOT NULL UNIQUE,
    source_app TEXT,
    favorite INTEGER NOT NULL DEFAULT 0,
    sensitive INTEGER NOT NULL DEFAULT 0,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    expires_at INTEGER
);

CREATE INDEX idx_clipboard_created_at ON clipboard_item(created_at);
CREATE INDEX idx_clipboard_content_type ON clipboard_item(content_type);

CREATE VIRTUAL TABLE clipboard_item_fts
USING fts5(content, content='clipboard_item', content_rowid='id');
```

敏感信息检测规则示例：

- JWT
- API Key
- Bearer Token
- GitHub Token
- AWS Access Key
- 私钥片段
- 数据库连接串

敏感内容默认仍可保存，但应提供配置：

- 不保存敏感内容
- 保存但搜索结果脱敏
- 保存完整内容

## 7. 插件系统设计

### 7.1 插件路线

不建议早期把开放插件生态建立在 `.dll` / `.so` / `.dylib` 动态库之上。

推荐三层插件模型：

```text
Built-in Tools
  Rust 原生模块，随主程序发布

Safe Plugins
  WASM 插件，适合开放生态

Process Plugins
  独立进程插件，通过 JSON-RPC / stdio 通信

Native Plugins
  原生动态库，仅用于官方或可信高性能插件
```

优先级：

```text
Phase 1: Built-in Tools
Phase 2: WASM Plugins
Phase 2.5: Process Plugins
Phase 3: Native Plugins
```

### 7.2 插件目录结构

WASM 插件：

```text
plugins/
└── json-tools/
    ├── plugin.toml
    └── plugin.wasm
```

进程插件：

```text
plugins/
└── api-client/
    ├── plugin.toml
    └── plugin.exe
```

原生插件：

```text
plugins/
└── native-codec/
    ├── plugin.toml
    └── plugin.dll
```

Linux 和 macOS 对应：

```text
plugin.so
plugin.dylib
```

### 7.3 plugin.toml 示例

```toml
[plugin]
id = "devtools.json"
name = "JSON Tools"
version = "0.1.0"
description = "JSON formatter, minifier and validator"
author = "DevTools Hub"
runtime = "wasm"
entry = "plugin.wasm"

[[commands]]
id = "json.format"
title = "Format JSON"
keywords = ["json", "format", "pretty"]
input = "text"

[[commands]]
id = "json.minify"
title = "Minify JSON"
keywords = ["json", "minify", "compress"]
input = "text"

[permissions]
clipboard = true
filesystem = false
network = false
shell = false
```

### 7.4 插件生命周期

```text
启动应用
  ↓
扫描 plugins 目录
  ↓
读取 plugin.toml
  ↓
校验版本、权限和运行时
  ↓
注册 commands
  ↓
参与搜索匹配
  ↓
用户执行 command
  ↓
Plugin Host 调用插件
  ↓
返回 CommandResult
  ↓
UI 展示结果
```

### 7.5 插件权限模型

插件默认无权限。

权限示例：

- clipboard.read
- clipboard.write
- filesystem.read
- filesystem.write
- network.request
- shell.execute
- notification.show

权限应在安装或首次执行时提示用户确认。

### 7.6 插件通信协议

插件输入：

```json
{
  "command": "json.format",
  "input": "{ \"hello\": \"world\" }",
  "context": {
    "theme": "dark",
    "platform": "windows",
    "locale": "zh-CN"
  }
}
```

插件输出：

```json
{
  "type": "text",
  "title": "Formatted JSON",
  "content": "{\n  \"hello\": \"world\"\n}",
  "actions": [
    {
      "id": "copy",
      "title": "Copy",
      "kind": "copy"
    }
  ]
}
```

## 8. 内置工具设计

内置工具作为 Rust crate 接入统一 Command 接口。

### 8.1 JSON

功能：

- 格式化
- 压缩
- 校验
- JSONPath 查询
- JSON 转 YAML
- YAML 转 JSON

### 8.2 JWT

功能：

- Header 解析
- Payload 解析
- exp / iat / nbf 时间转换
- 签名算法识别
- 过期状态提示

默认不做在线校验，避免泄露 Token。

### 8.3 编码工具

功能：

- Base64 Encode
- Base64 Decode
- URL Encode
- URL Decode
- Hex Encode
- Hex Decode

### 8.4 时间工具

功能：

- Unix 时间戳转本地时间
- Unix 时间戳转 UTC
- ISO 8601 解析
- 当前时间戳
- 时区转换

### 8.5 UUID

功能：

- UUID v4 生成
- 批量生成
- 大写/小写转换
- 去横线/加横线

### 8.6 SQL Formatter

功能：

- SQL 格式化
- SQL 压缩
- 基础语法高亮

复杂 SQL 解析不作为早期目标。

## 9. UI 设计原则

### 9.1 体验原则

- 暗色主题优先
- 键盘优先
- 搜索即执行
- 结果可复制
- 工具切换低成本
- 常用操作不打断输入流

### 9.2 主窗口

主窗口类似 Raycast：

```text
┌────────────────────────────────────────┐
│ Search tools, commands, clipboard...   │
├────────────────────────────────────────┤
│ JSON Format                            │
│ Format and validate JSON               │
├────────────────────────────────────────┤
│ Clipboard: Bearer eyJ...               │
│ 2 minutes ago                          │
├────────────────────────────────────────┤
│ UUID Generator                         │
│ Generate random UUID v4                │
└────────────────────────────────────────┘
```

### 9.3 工具页面

工具页面分为：

- 输入区
- 输出区
- 操作栏
- 结果状态

示例：

```text
┌──────────────────────────────────────────────┐
│ JSON Format                         Copy Run │
├──────────────────────┬───────────────────────┤
│ Input                │ Output                │
│ { "a": 1 }           │ {                     │
│                      │   "a": 1              │
│                      │ }                     │
└──────────────────────┴───────────────────────┘
```

### 9.4 主题

至少支持：

- Dark
- Light
- Follow System

主题配置保存到 SQLite。

## 10. 配置与数据目录

建议使用平台标准目录：

Windows:

```text
%APPDATA%/DevToolsHub/
```

macOS:

```text
~/Library/Application Support/DevToolsHub/
```

Linux:

```text
~/.config/devtools-hub/
```

目录结构：

```text
DevToolsHub/
├── devtools.db
├── config.toml
├── plugins/
├── logs/
└── cache/
```

## 11. 数据库设计

### 11.1 settings

```sql
CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at INTEGER NOT NULL
);
```

### 11.2 plugin

```sql
CREATE TABLE plugin (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    version TEXT NOT NULL,
    runtime TEXT NOT NULL,
    path TEXT NOT NULL,
    enabled INTEGER NOT NULL DEFAULT 1,
    installed_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);
```

### 11.3 command

```sql
CREATE TABLE command (
    id TEXT PRIMARY KEY,
    plugin_id TEXT,
    title TEXT NOT NULL,
    keywords TEXT,
    source TEXT NOT NULL,
    enabled INTEGER NOT NULL DEFAULT 1,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);
```

### 11.4 execution_history

```sql
CREATE TABLE execution_history (
    id INTEGER PRIMARY KEY,
    command_id TEXT NOT NULL,
    input_hash TEXT,
    executed_at INTEGER NOT NULL
);

CREATE INDEX idx_execution_history_command_id
ON execution_history(command_id);

CREATE INDEX idx_execution_history_executed_at
ON execution_history(executed_at);
```

## 12. 跨平台风险

### 12.1 Windows

重点验证：

- 全局快捷键
- 托盘菜单
- 剪贴板监听
- 开机自启动
- 安装包签名
- 高 DPI

### 12.2 macOS

重点验证：

- 快捷键权限
- 辅助功能权限
- 通知权限
- 托盘状态栏
- 应用签名与公证
- Command + Space 冲突

### 12.3 Linux

重点验证：

- X11 和 Wayland 差异
- 全局快捷键可用性
- 剪贴板可用性
- AppIndicator / StatusNotifier 托盘兼容性
- AppImage / deb / rpm 打包

Linux Wayland 是最高风险平台，应在 Phase 1 就做技术验证。

## 13. 开发阶段

### Phase 0: 技术验证

目标：验证项目不可控风险。

任务：

- Slint 搜索窗口原型
- 全局快捷键唤起窗口
- SQLite 初始化和 migration
- 剪贴板采集
- Tokio 与 Slint 事件通信
- Windows/macOS/Linux 最小运行验证

验收：

- 快捷键可以唤起窗口
- 输入框自动聚焦
- 剪贴板内容可以入库
- 搜索结果可以展示
- UI 不被后台任务阻塞

### Phase 1: MVP

目标：完成可日常使用的核心工具。

任务：

- 主搜索窗口
- 内置 JSON 工具
- 内置 Base64 工具
- 剪贴板历史
- SQLite FTS 搜索
- 设置页
- 暗色/亮色主题
- 托盘退出

验收指标：

- 冷启动小于 800ms
- 快捷键唤起小于 120ms
- 搜索响应小于 50ms
- 空闲内存小于 100MB
- 10 万条剪贴板记录可搜索

### Phase 2: 插件系统

目标：建立安全可扩展的插件模型。

任务：

- plugin.toml manifest
- 插件扫描
- 插件注册命令
- WASM 插件宿主
- 插件权限模型
- JSON-RPC 或结构化调用协议
- 插件错误隔离

内置工具扩展：

- JWT
- URL Encode/Decode
- 时间工具
- UUID
- SQL Formatter

### Phase 3: 高级能力

目标：扩展为完整开发者效率平台。

任务：

- API 调试工具
- SSH 工具
- 端口管理
- 正则测试
- Cron 解析
- AI 助手
- 插件市场
- 插件更新机制
- 云同步或局域网同步

## 14. 性能策略

### 14.1 启动性能

- 延迟扫描非必要插件
- 延迟加载重型工具
- SQLite 连接初始化轻量化
- 主窗口优先显示，再加载次要数据

### 14.2 搜索性能

- 使用 FTS5
- 内存缓存高频命令
- 剪贴板搜索分页
- 搜索输入防抖
- 执行历史参与排序

### 14.3 内存控制

- 大文本按需加载
- 剪贴板列表只加载摘要
- 插件执行后释放上下文
- WASM 插件限制内存
- 避免长期持有大字符串

## 15. 安全策略

### 15.1 剪贴板安全

- 敏感内容检测
- 敏感内容脱敏展示
- 可配置是否保存敏感内容
- 支持自动过期删除
- 支持一键清空历史

### 15.2 插件安全

- 默认无权限
- 权限显式声明
- 首次使用权限确认
- 插件执行超时
- 插件资源限制
- 插件崩溃不影响主进程
- 高危能力单独确认，例如 shell.execute

### 15.3 本地数据安全

- 数据库保存到用户应用目录
- 后续可选数据库加密
- 日志不记录敏感输入
- 崩溃报告默认不上传用户内容

## 16. 推荐依赖方向

候选依赖：

- slint: GUI
- tokio: 异步运行时
- rusqlite: SQLite
- serde / serde_json: 序列化
- toml: plugin.toml 解析
- directories: 平台标准目录
- tracing / tracing-subscriber: 日志
- anyhow / thiserror: 错误处理
- uuid: UUID 工具
- base64: Base64 工具
- jsonwebtoken: JWT 解析
- sqlformat: SQL 格式化
- wasmtime 或 extism: WASM 插件宿主
- global-hotkey: 全局快捷键候选
- arboard: 剪贴板候选
- notify-rust: 通知候选

注意：系统服务类依赖必须逐个平台验证，不能只看 crate API。

## 17. 关键决策记录

### ADR-001: 使用 Slint 而不是 Electron/WebView/Tauri

原因：

- 满足原生 GUI 目标
- 降低内存占用
- 避免引入浏览器运行时
- 更贴近桌面工具定位

代价：

- 插件不能直接复用 Web UI 生态
- 复杂文本编辑能力需要额外设计
- 更多平台细节需要自行处理

### ADR-002: 插件优先使用 WASM，而不是动态库

原因：

- 更容易隔离崩溃
- 更容易控制权限
- 更适合开放生态
- 跨平台分发成本低于动态库

代价：

- 插件能力需要通过宿主 API 暴露
- 复杂 UI 插件需要单独设计交互模型
- 性能极限低于原生动态库

### ADR-003: 搜索索引优先使用 SQLite FTS5

原因：

- 简化架构
- 避免早期自研搜索
- 足够支撑 MVP 数据量
- 方便和本地数据保持一致

代价：

- 高级排序和模糊匹配能力有限
- 后续可能需要自定义 ranking

## 18. Phase 1 最小可行边界

Phase 1 必须完成：

- 应用启动
- 搜索窗口
- 全局快捷键
- 剪贴板历史
- 本地搜索
- JSON 工具
- Base64 工具
- 设置存储
- 托盘退出

Phase 1 暂不做：

- 插件市场
- AI 助手
- SSH 工具
- API 调试工具
- 原生动态库插件
- 云同步
- 复杂多窗口工作区

## 19. 成功标准

产品层面：

- 开发者可以用快捷键快速打开工具
- 常见文本转换不需要打开浏览器
- 剪贴板历史稳定可搜索
- 常用工具可以通过输入关键词快速执行

工程层面：

- UI 和业务逻辑分离
- 系统能力被服务层封装
- 内置工具和插件走统一命令协议
- 后台任务不会阻塞 UI
- 数据库 schema 可迁移
- 三个平台都能构建和运行

## 20. 总结

DevTools Hub 的技术方向成立。Rust + Slint + SQLite + Tokio 可以支撑一个低内存、原生、跨平台的开发者工具。

项目真正的难点不是内置工具，而是：

- 跨平台系统能力
- UI 线程和后台任务协调
- 插件安全边界
- Linux Wayland 兼容性
- macOS 权限和签名

因此开发顺序应该是：

```text
先验证系统边界
  ↓
再完成核心体验
  ↓
再扩展内置工具
  ↓
最后开放插件生态
```

这样可以尽早暴露不可控风险，避免在工具功能做完之后才发现底层平台能力不稳定。
